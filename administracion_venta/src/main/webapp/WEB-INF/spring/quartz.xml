<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd">
    <!-- Jobs -->
    <bean id="creaOrdenesRecolDiarias" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="solicitudRecoleccionBO" />
        <property name="targetMethod" value="generarOrdenesRecoleccion" />
        <property name="concurrent" value="false" />
    </bean>
    <!-- Cambia el estado de los password para verificar que caduquen cada 60 dias -->
     <bean id="caducidadPasswords" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="adminPasswordBO" />
        <property name="targetMethod" value="cambiaEstatus" />
        <property name="concurrent" value="false" />
    </bean>
    
    
    <!-- Crons -->
    <bean id="cronCreaOrdenesRecolDiarias" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="creaOrdenesRecolDiarias" />
        <!-- run every hour -->
        <property name="cronExpression" value="0 2 2,14 * * ?" />
    </bean>
    
    <!-- Se correo el proceso de password todos los días a las 12 esta es la expesion que debe 
    esatr en PRO 0 59 23 ? * * *
     -->
     <bean id="cronCaducidadPasswords" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="caducidadPasswords" />
        <!-- run every hour -->
        <property name="cronExpression" value="0 0/5 * * * ?" /> 
    </bean>
    
    <!-- Scheduler -->
    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="cronCreaOrdenesRecolDiarias" />
                 <ref bean="cronCaducidadPasswords" />
            </list>
        </property>
        <property name="overwriteExistingJobs" value="true"/>    	
    	<property name="startupDelay" value="10" /> 
	    <property name="autoStartup" value="true"/>
    </bean>
</beans>